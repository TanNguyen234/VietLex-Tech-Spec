[pytest]
testpaths = tests
asyncio_default_fixture_loop_scope = function
markers =
    live: calls real cloud providers and is skipped unless explicitly enabled
filterwarnings =
    ignore:No logs or spans will be created.*:logfire._internal.config.LogfireNotConfiguredWarning
