# Complete the research workflow

Scope: runtime/API, user interface and documentation; no ingestion, migration or model/ranker changes. User authorizes implementation, live calls, commits, push and deployment. Execute inline without further questions. CRG unavailable; validate source directly.

1. Source library: derive grouped, version-separated page coverage from retained trusted-source analyses. A page counts only with nonempty extracted text; a title or fetched blank page does not count. GET `/workspaces/{workspace_id}/sources/{analysis_id}` reopens stored reads without a provider call. Test same-hash windows, changed hash, blank page and ownership before implementation.
2. Evidence review: POST `/workspaces/{workspace_id}/evidence/{evidence_id}/review` stores status `to_check`, `text_checked` or `follow_up`, note and optimistic revision separately from raw quote. Atomic Mongo predicate includes owner, expiry, evidence ID and prior revision. No legal-effect promotion. Test stale revision, absent/other-owner evidence, preserved raw text and expiry predicate before implementation.
3. Workspace UX: source library and next-step overview; compact evidence with visible unverified status and page provenance; contextual selected-source actions; searchable evidence, retained selection across tabs, mobile layouts. Reader supports selecting text to pin, page-window controls and readable error messages. Verify browser selection, reload, keyboard and 390/1440 widths.
4. Findings/reports: readable findings with source links and local status filters; safe server-rendered report preview with source anchors and print layout, edit/save remains versioned. Escape arbitrary HTML, reject dangerous link protocols, preserve raw Markdown and export contracts. Focused tests precede behavior changes.
5. Review final diff and failure paths. Run affected tests during implementation; run full provider-free suite once after stable source. Live authenticated source reopen/review and report workflow use existing real Mongo/Vertex setup; preserve provider failures. Generate final screenshots/report after stability, stage only task paths, split commits, push and verify remote state.

Commands: `.venv/Scripts/python.exe -m pytest tests/test_source_library.py tests/test_evidence_review.py tests/test_report_deliverables.py tests/test_workspace_routes.py tests/test_trusted_source_routes.py -q -p no:cacheprovider`; `node --test tests/product_forms.test.cjs`; final `.venv/Scripts/python.exe -m pytest tests --ignore=tests/integration --ignore=tests/visual -q -p no:cacheprovider`. TDD tests themselves define exact assertions and failure cases; browser/live artifacts are generated only after code is stable.

Implementation tasks 1–4 completed. Verification and release evidence: `docs/verification/workflow-usability-20260912/REPORT.md`. This bounded plan does not claim completion of the original corpus/index roadmap.
