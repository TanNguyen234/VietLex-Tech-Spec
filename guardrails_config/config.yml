# config.yml
# NVIDIA NeMo Guardrails Configuration

models:
  - type: main
    engine: openai
    model: legal-core-model
    parameters:
      base_url: http://127.0.0.1:4000/v1

rails:
  input:
    flows:
      - self check input
  output:
    flows:
      - self check facts custom
